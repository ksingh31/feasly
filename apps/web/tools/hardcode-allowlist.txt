# Hardcode allowlist — files exempt from tools/check-no-hardcode.mjs.
# One repo-relative path per line. Every entry MUST have a reason comment.
# "Rare justified exception" only: the default is to put the value in config.

# The single intentional home for tunable literals: compiled fallback defaults
# for when /assets/config/app-config.json is missing or malformed (FE0-002).
apps/web/src/app/core/config/app-config.defaults.ts

# Canned fake data for the mock API harness (FE0-003): fixture figures and
# copy are inherently literal — the conformance specs pin every value.
apps/web/src/app/core/api/mock-data.ts

# Legal prose (FE1-001): privacy/terms change by legal review, not by deploy
# tuning — stuffing them in app-config.json would bypass that. Copy-pinned by
# the landing spec's route tests (pages render, links resolve).
apps/web/src/app/features/legal/privacy-page.component.ts
apps/web/src/app/features/legal/terms-page.component.ts

# Branded 404 copy (SEO-01): the exact strings are pinned by the story's
# acceptance criteria — they change by product decision, not by deploy
# tuning. Copy-pinned by not-found-page.component.spec.ts.
apps/web/src/app/features/not-found/not-found-page.component.ts

# The `/404` route path (SEO-01): 404 is the HTTP status code — a web
# standard, not a tunable. The no-hardcode tripwire's magic-number rule
# cannot distinguish it from a tunable threshold.
apps/web/src/app/app.routes.ts
apps/web/src/app/core/seo/seo-routes.ts

# Branded error + offline pages (HRD-02): the exact strings are pinned by the
# story's acceptance criteria — they change by product decision, not by
# deploy tuning. Copy-pinned by error-page.component.spec.ts and
# offline-page.component.spec.ts.
apps/web/src/app/features/error/error-page.component.ts
apps/web/src/app/features/error/offline-page.component.ts

# Labelled sample report (seo/09): canned fictional data — the address,
# figures, and banner copy are inherently literal, exactly like mock-data.ts
# above. Every value is pinned by sample-report-page.component.spec.ts.
apps/web/src/app/features/sample-report/sample-report-page.component.ts
apps/web/src/app/features/sample-report/sample-report-page.component.html

# FAQ JSON-LD context (SEO-010): `https://schema.org` is the fixed
# vocabulary URI for structured data — a web standard, not a tunable
# (same category as the /404 precedent above).
apps/web/src/app/features/marketing/faq-page.component.ts

# Landing WebSite+FAQPage JSON-LD context (SEO-06): same `https://schema.org`
# fixed vocabulary URI as the FAQ page above — a web standard, not a tunable.
apps/web/src/app/features/landing/landing-page.component.ts

# API docs prose (api-mcp/03): endpoint docs, code samples, and the changelog
# change with the API (code review), not by deploy tuning — same category as
# the legal pages above. Copy-pinned by developers-page.component.spec.ts.
apps/web/src/app/features/developers/developers-page.component.ts
apps/web/src/app/features/developers/developers-page.component.html
