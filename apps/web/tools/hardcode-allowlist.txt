# Hardcode allowlist — files exempt from tools/check-no-hardcode.mjs.
# One repo-relative path per line. Every entry MUST have a reason comment.
# "Rare justified exception" only: the default is to put the value in config.

# The single intentional home for tunable literals: compiled fallback defaults
# for when /assets/config/app-config.json is missing or malformed (FE0-002).
apps/web/src/app/core/config/app-config.defaults.ts

# Canned fake data for the mock API harness (FE0-003): fixture figures and
# copy are inherently literal — the conformance specs pin every value.
apps/web/src/app/core/api/mock-data.ts

# Legal prose (FE1-001): privacy/terms change by legal review, not by deploy
# tuning — stuffing them in app-config.json would bypass that. Copy-pinned by
# the landing spec's route tests (pages render, links resolve).
apps/web/src/app/features/legal/privacy-page.component.ts
apps/web/src/app/features/legal/terms-page.component.ts

# Labelled sample report (seo/09): canned fictional data — the address,
# figures, and banner copy are inherently literal, exactly like mock-data.ts
# above. Every value is pinned by sample-report-page.component.spec.ts.
apps/web/src/app/features/sample-report/sample-report-page.component.ts
apps/web/src/app/features/sample-report/sample-report-page.component.html
