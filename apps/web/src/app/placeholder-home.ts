import { Component } from '@angular/core';

/**
 * Scaffold placeholder for `/`. Replaced by the real landing page in FE1-001.
 * Exists only so the prerender pipeline has a route to render.
 */
@Component({
  selector: 'app-placeholder-home',
  template: `<h1>Feasly</h1><p>Scaffold placeholder — the landing page lands in FE1-001.</p>`,
})
export class PlaceholderHome {}
