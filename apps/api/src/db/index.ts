/**
 * Database layer: Drizzle schema, migrations, and client construction.
 *
 * BE0-001: intentionally empty — BE-1 introduces the Drizzle schema,
 * migrations, and the client factory here.
 *
 * Hard rule (enforced by test/boundaries.test.ts): NOTHING outside
 * src/services/ and src/composition.ts may import from this directory.
 */
export {};
