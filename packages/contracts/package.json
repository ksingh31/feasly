{
  "name": "@feasly/contracts",
  "version": "0.3.0",
  "description": "Shared TypeScript DTO shapes for the Feasly REST API and embed postMessage protocol. Types only — no runtime code, no validation schemas.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "tsc --noEmit -p tsconfig.typecheck.json",
    "lint": "node scripts/check-no-any.js"
  }
}
