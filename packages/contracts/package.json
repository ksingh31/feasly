{
  "name": "@feasly/contracts",
  "version": "0.1.0",
  "description": "Shared DTOs + validation schemas for REST and postMessage (epics 03, 05, 07, 10).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "tsc --noEmit -p tsconfig.typecheck.json",
    "lint": "node scripts/check-no-any.js"
  }
}
