# HRD-07 secrets-scan allowlist.
# Format: one path glob per line, each with a "# reason" comment directly
# above it. check-secrets.mjs fails closed (exit 2) when an entry has no
# reason. Only test fixtures may be allowlisted — a real credential is
# never allowlisted; rotate it instead (see docs/ops/secrets.md).

# Deliberately fake secret-shaped fixture strings used by the scanner's own
# test suite to prove the gate fails on them (HRD-07 AC1). Every value is an
# obvious placeholder (fake/test/dummy material), never a real credential.
tools/check-secrets.test.mjs
