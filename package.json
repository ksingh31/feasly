{
  "name": "feasly",
  "version": "0.1.0",
  "private": true,
  "description": "Feasly — deterministic infill build-cost estimator for Calgary (builder SaaS embed + marketplace).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "prettier": "^3.3.3",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
